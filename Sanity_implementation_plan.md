[ ] NAME:Current Task List DESCRIPTION:Root task for conversation __NEW_AGENT__
-[ ] NAME:Phase 0: Sanity Setup & Configuration DESCRIPTION:Set up Sanity CMS infrastructure, install dependencies, configure project, and establish folder structure for schemas.
--[ ] NAME:Install Sanity packages DESCRIPTION:Run: npm install sanity @sanity/vision next-sanity @sanity/image-url. Install all required Sanity v3 dependencies for Next.js integration.
--[ ] NAME:Initialize Sanity project DESCRIPTION:Run: npx sanity init --env (if new project) OR configure existing project. Get project ID and dataset name. Store in .env.local as NEXT_PUBLIC_SANITY_PROJECT_ID and NEXT_PUBLIC_SANITY_DATASET.
--[ ] NAME:Create sanity.config.ts DESCRIPTION:Create /sanity.config.ts with project configuration including projectId, dataset, apiVersion, plugins (deskTool, visionTool), and schema imports. Set up for bilingual content.
--[ ] NAME:Create schema folder structure DESCRIPTION:Create folders: /sanity/schemas/documents/, /sanity/schemas/objects/, /sanity/schemas/shared/. Create /sanity/schemas/index.ts to export all schemas.
--[ ] NAME:Set up Sanity Studio route DESCRIPTION:Create /app/studio/[[...index]]/page.tsx using NextStudio component from 'sanity'. This allows editing content at /studio route.
--[ ] NAME:Configure environment variables DESCRIPTION:Add to .env.local: NEXT_PUBLIC_SANITY_PROJECT_ID, NEXT_PUBLIC_SANITY_DATASET, SANITY_API_TOKEN (for writes), NEXT_PUBLIC_SANITY_API_VERSION (e.g., 2024-01-01).
-[ ] NAME:Phase 1: Content Inventory & Mapping DESCRIPTION:Audit all existing pages and components to identify every text string, image, and content block. Map each to appropriate Sanity schema types. This content will become the default seed data in Sanity.
--[ ] NAME:Scan app directory for all pages DESCRIPTION:Use codebase-retrieval and view tools to scan app/[locale]/** for all page.tsx files. List: home, solutions, apps, about, contact, blog, faq, legal pages, etc.
--[ ] NAME:Scan components for shared content DESCRIPTION:Scan components/** for Navigation, Footer, Hero sections, Feature cards, Testimonials, Stats sections, CTA sections, Banners. Identify all text/images.
--[ ] NAME:Audit translation files DESCRIPTION:Review messages/en.json and messages/ar.json. Extract all user-facing content that should move to Sanity (page content, not system UI strings).
--[ ] NAME:Identify static data files DESCRIPTION:Search for any /data/*.ts or /constants/*.ts files containing content arrays (solutions, apps, features, team members, etc.).
--[ ] NAME:Create content inventory document DESCRIPTION:Create docs/content-inventory.md with tables listing: Location, Content Type, Context, Target Sanity Schema, Localization Status, Current EN/AR values.
--[ ] NAME:Map content to schema types DESCRIPTION:For each inventory item, assign target schema (e.g., homePage.hero, solution, faqItem, navigation.mainNavItems). This mapping guides Phase 3 migration.
-[ ] NAME:Phase 2: Implement Sanity Schemas DESCRIPTION:Create all Sanity schema types including localized objects, document types, and SEO fields. Build the complete content model.
--[ ] NAME:Create localized field types DESCRIPTION:Create /sanity/schemas/objects/localizedString.ts, localizedText.ts, localizedRichText.ts. Each has {en: string/text/array, ar: string/text/array} structure with proper validation.
--[ ] NAME:Create SEO object schema DESCRIPTION:Create /sanity/schemas/objects/seo.ts with fields: metaTitle (localized), metaDescription (localized), ogTitle, ogDescription, ogImage, canonicalUrl, indexable, structuredData.
--[ ] NAME:Create shared object schemas DESCRIPTION:Create objects: heroSection, featureSection, featureItem, ctaSection, ctaLink, navItem, statsSection, testimonialsSection, testimonialItem, richTextSection, timelineItem, valueItem, locationItem. All in /sanity/schemas/objects/.
--[ ] NAME:Create siteSettings schema DESCRIPTION:Create /sanity/schemas/documents/siteSettings.ts as singleton. Fields: title, description, logo, favicon, defaultSeo, contactEmail, contactPhone, socialLinks. Mark as singleton in schema.
--[ ] NAME:Create navigation schema DESCRIPTION:Create /sanity/schemas/documents/navigation.ts as singleton. Fields: mainNavItems (array of navItem), secondaryNavItems, footerNavColumns, ctaButtons, localeSwitchLabels.
--[ ] NAME:Create page schemas (singletons) DESCRIPTION:Create: homePage.ts, aboutPage.ts, contactPage.ts, faqPage.ts, errorPage.ts. Each with: title, slug, sections array (polymorphic), seo field. Mark as singletons.
--[ ] NAME:Create collection schemas DESCRIPTION:Create: solution.ts, solutionCategory.ts, app.ts, blogPost.ts, blogCategory.ts, author.ts, faqItem.ts, faqCategory.ts, legalPage.ts, teamMember.ts, genericPage.ts. All with proper fields and seo.
--[ ] NAME:Add validation and descriptions DESCRIPTION:For all schemas, add: validation rules (required fields), field descriptions for editors, fieldsets for grouping (e.g., SEO group), preview configurations.
--[ ] NAME:Register all schemas DESCRIPTION:Export all schemas in /sanity/schemas/index.ts as schemaTypes array. Import in sanity.config.ts. Test that Studio loads without errors.
-[ ] NAME:Phase 3: Migrate Existing Content to Sanity DESCRIPTION:Extract all current hardcoded content from the codebase and populate it as default content in Sanity. This includes all English/Arabic text, images, and structured data.
--[ ] NAME:Start Sanity Studio locally DESCRIPTION:Run: npm run dev and navigate to /studio. Verify all schemas load correctly and Studio UI is functional.
--[ ] NAME:Create singleton documents DESCRIPTION:In Studio, create: siteSettings, navigation, homePage, aboutPage, contactPage, faqPage. Leave fields empty for now - will populate next.
--[ ] NAME:Extract and migrate siteSettings content DESCRIPTION:From content inventory, extract global site data (title, description, logo, social links, contact info). Populate siteSettings document with EN/AR values from current codebase.
--[ ] NAME:Extract and migrate navigation content DESCRIPTION:From current Navigation/Header/Footer components, extract all menu items, labels, links. Populate navigation document with mainNavItems, footerNavColumns, ctaButtons in both EN/AR.
--[ ] NAME:Extract and migrate home page content DESCRIPTION:From app/[locale]/page.tsx and related components, extract hero, features, stats, testimonials, CTAs. Populate homePage document with all sections in EN/AR.
--[ ] NAME:Extract and migrate solutions content DESCRIPTION:From solutions page/data, create solution documents for each solution. Include title, description, features, images in EN/AR. Create solutionCategory documents if categories exist.
--[ ] NAME:Extract and migrate apps content DESCRIPTION:From apps page/data, create app documents for each app. Include name, tagline, description, platforms, store URLs, screenshots in EN/AR.
--[ ] NAME:Extract and migrate about page content DESCRIPTION:From about page, extract story, timeline, values, team members. Populate aboutPage document and create teamMember documents with EN/AR content.
--[ ] NAME:Extract and migrate contact page content DESCRIPTION:From contact page, extract intro text, form labels, office locations. Populate contactPage document with EN/AR content.
--[ ] NAME:Extract and migrate FAQ content DESCRIPTION:From FAQ page/data, create faqItem documents for each question/answer. Create faqCategory documents if needed. Populate faqPage with intro and featured FAQs in EN/AR.
--[ ] NAME:Extract and migrate blog content DESCRIPTION:If blog exists, create blogPost documents with title, excerpt, body, cover image in EN/AR. Create author and blogCategory documents as needed.
--[ ] NAME:Extract and migrate legal pages content DESCRIPTION:From legal pages (terms, privacy, etc.), create legalPage documents with full body content in EN/AR.
--[ ] NAME:Add SEO metadata to all documents DESCRIPTION:For each created document, populate seo fields: metaTitle, metaDescription, ogImage, etc. in both EN/AR. Use existing meta tags from pages as source.
--[ ] NAME:Upload and reference images DESCRIPTION:Upload all content images from /public to Sanity assets. Update all image references in documents to use Sanity-hosted images.
-[ ] NAME:Phase 4: Next.js Sanity Client & Query Layer DESCRIPTION:Configure Sanity client for Next.js, create GROQ queries with localization support, implement caching and revalidation strategies.
--[ ] NAME:Create Sanity client configuration DESCRIPTION:Create /lib/sanity/client.ts with createClient from next-sanity. Configure projectId, dataset, apiVersion, useCdn settings. Export client for server components.
--[ ] NAME:Create preview client (optional) DESCRIPTION:Create /lib/sanity/previewClient.ts for draft content preview. Configure with token and useCdn: false. Create preview mode API route at /app/api/preview/route.ts.
--[ ] NAME:Create GROQ query utilities DESCRIPTION:Create /lib/sanity/queries.ts with functions: getSiteSettings(locale), getNavigation(locale), getHomePage(locale), getSolutions(locale), getApps(locale), etc. Use coalesce for locale fallback.
--[ ] NAME:Implement localization query patterns DESCRIPTION:In all GROQ queries, project localized fields as: 'title': coalesce(title[$locale], title.en), 'description': coalesce(description[$locale], description.en). Create reusable projection fragments.
--[ ] NAME:Create TypeScript types from schemas DESCRIPTION:Create /lib/sanity/types.ts with TypeScript interfaces matching Sanity schemas. Include: SiteSettings, Navigation, HomePage, Solution, App, BlogPost, etc. with localized field types.
--[ ] NAME:Configure caching strategy DESCRIPTION:In client.ts, configure fetch options. For pages, use export const revalidate = 3600 (1 hour). Plan webhook-based revalidation for instant updates. Document strategy in /docs/sanity-caching.md.
--[ ] NAME:Create image URL builder utility DESCRIPTION:Create /lib/sanity/image.ts using @sanity/image-url. Export imageUrlBuilder function to generate optimized image URLs from Sanity image references.
--[ ] NAME:Test queries in Vision plugin DESCRIPTION:Use Sanity Studio Vision plugin to test all GROQ queries with both en and ar locales. Verify coalesce fallbacks work correctly.
-[ ] NAME:Phase 5: Page-by-Page Content Migration DESCRIPTION:Replace hardcoded content in each page component with Sanity-driven data. Wire up all pages to fetch from CMS instead of static sources.
--[ ] NAME:Migrate home page to Sanity DESCRIPTION:Update app/[locale]/page.tsx to fetch homePage data via getHomePage(locale). Replace all hardcoded hero, sections, features with Sanity data. Update Hero, Features, Stats, Testimonials components to accept Sanity props.
--[ ] NAME:Migrate solutions listing page DESCRIPTION:Update app/[locale]/solutions/page.tsx to fetch solutions via getSolutions(locale). Map over solution documents to render cards. Update SolutionCard component to use Sanity data structure.
--[ ] NAME:Create solution detail page DESCRIPTION:Create/update app/[locale]/solutions/[slug]/page.tsx. Implement generateStaticParams for all solution slugs. Fetch single solution by slug. Render hero, body, features from Sanity.
--[ ] NAME:Migrate apps listing page DESCRIPTION:Update app/[locale]/apps/page.tsx to fetch apps via getApps(locale). Map over app documents. Update AppCard component to use Sanity data (name, tagline, platforms, store URLs).
--[ ] NAME:Create app detail page DESCRIPTION:Create/update app/[locale]/apps/[slug]/page.tsx. Implement generateStaticParams. Fetch single app by slug. Render description, features, screenshots, platform-specific CTAs from Sanity.
--[ ] NAME:Migrate about page DESCRIPTION:Update app/[locale]/about/page.tsx to fetch aboutPage via getAboutPage(locale). Fetch referenced teamMember documents. Render story, timeline, values, team grid from Sanity.
--[ ] NAME:Migrate contact page DESCRIPTION:Update app/[locale]/contact/page.tsx to fetch contactPage. Use intro, locations from Sanity. Keep form submission logic in code. Update form labels/placeholders to use Sanity data if needed.
--[ ] NAME:Migrate FAQ page DESCRIPTION:Update app/[locale]/faq/page.tsx to fetch faqPage and faqItems. Render intro, categories, accordion items from Sanity. Update FAQ accordion component to use Sanity data structure.
--[ ] NAME:Create blog listing page DESCRIPTION:Create/update app/[locale]/blog/page.tsx to fetch blogPosts via getBlogPosts(locale). Render post cards with title, excerpt, cover image, author, date from Sanity.
--[ ] NAME:Create blog post detail page DESCRIPTION:Create/update app/[locale]/blog/[slug]/page.tsx. Implement generateStaticParams. Fetch single blogPost by slug. Render title, cover, body (rich text), author, categories from Sanity.
--[ ] NAME:Create legal pages dynamic route DESCRIPTION:Create app/[locale]/legal/[slug]/page.tsx. Implement generateStaticParams for all legalPage slugs. Fetch legalPage by slug. Render title and body (rich text) from Sanity.
--[ ] NAME:Create generic pages route (optional) DESCRIPTION:If needed, create app/[locale]/[slug]/page.tsx for genericPage documents. Fetch by slug, render title and body. Ensure this doesn't conflict with other routes.
--[ ] NAME:Update all components to use Sanity image URLs DESCRIPTION:Replace all Image src props with imageUrlBuilder from /lib/sanity/image.ts. Update all components that display images to use Sanity CDN URLs with proper sizing.
-[ ] NAME:Phase 6: Global Navigation, Footer & System Pages DESCRIPTION:Migrate navigation menus, footer content, error pages, and announcement banners to Sanity.
--[ ] NAME:Migrate navigation to Sanity DESCRIPTION:Update Navigation/Header component to fetch navigation via getNavigation(locale). Replace hardcoded menu items, CTAs, locale switcher labels with Sanity data. Handle nested menu items if present.
--[ ] NAME:Migrate footer to Sanity DESCRIPTION:Update Footer component to fetch navigation.footerNavColumns and siteSettings (social links, contact). Replace all hardcoded footer content with Sanity data.
--[ ] NAME:Migrate error pages to Sanity DESCRIPTION:Update app/[locale]/not-found.tsx and app/[locale]/error.tsx to fetch errorPage document. Use title, description, CTAs from Sanity. Keep error handling logic in code.
--[ ] NAME:Migrate announcement banner (if exists) DESCRIPTION:If announcement/promo banner exists, add announcement field to siteSettings schema. Update banner component to fetch and display from Sanity with dismissal logic.
-[ ] NAME:Phase 7: SEO & Meta Integration DESCRIPTION:Implement SEO metadata generation from Sanity, create sitemap and robots.txt, optionally add dynamic OG images.
--[ ] NAME:Create SEO metadata helper DESCRIPTION:Create /lib/seo.ts with generateMetadata helper function. Takes Sanity seo object + locale, returns Next.js Metadata object with title, description, openGraph, twitter, canonical, robots.
--[ ] NAME:Implement generateMetadata for all pages DESCRIPTION:For each page route (home, solutions, apps, about, contact, faq, blog, legal), export async generateMetadata function that fetches page data and uses generateMetadata helper to return SEO metadata.
--[ ] NAME:Create sitemap.ts DESCRIPTION:Create app/sitemap.ts. Query Sanity for all public documents (solutions, apps, blogPosts, legalPages, genericPages). Generate sitemap entries for both /en and /ar routes with lastModified dates.
--[ ] NAME:Create robots.ts DESCRIPTION:Create app/robots.ts. Return robots config allowing all crawlers, disallowing /studio, referencing sitemap URL. Use environment variable for production vs staging rules.
--[ ] NAME:Implement structured data (JSON-LD) DESCRIPTION:For pages with structuredData in seo object, inject <script type='application/ld+json'> in page metadata. Support Organization, WebSite, Article, FAQPage schemas.
--[ ] NAME:Create dynamic OG images (optional) DESCRIPTION:Create app/[locale]/blog/[slug]/opengraph-image.tsx and similar for solutions/apps. Use @vercel/og to generate branded OG images with title overlay. Integrate into metadata.
--[ ] NAME:Test SEO metadata DESCRIPTION:Use browser dev tools and online tools (Google Rich Results Test, Twitter Card Validator, Facebook Debugger) to verify all meta tags, OG images, structured data for both EN/AR pages.
-[ ] NAME:Phase 8: Testing & Validation DESCRIPTION:Comprehensive testing of all CMS-driven content, SEO metadata, localization, caching, and editorial workflows.
--[ ] NAME:Test all pages render with Sanity data DESCRIPTION:Visit every page route (/en and /ar) and verify content displays correctly from Sanity. Check hero sections, body content, images, links, CTAs all work.
--[ ] NAME:Test locale switching DESCRIPTION:For each page type, switch between /en and /ar routes. Verify correct language content displays, RTL layout applies for Arabic, and no content mixing occurs.
--[ ] NAME:Test navigation and footer DESCRIPTION:Verify navigation menus, footer links, CTAs all work. Update a menu item in Sanity Studio and confirm it reflects on site after revalidation.
--[ ] NAME:Test SEO metadata DESCRIPTION:Inspect page source for meta tags. Verify title, description, OG tags, canonical URLs are correct for both locales. Test with SEO tools (Google Rich Results, Twitter Card Validator).
--[ ] NAME:Test sitemap and robots.txt DESCRIPTION:Visit /sitemap.xml and verify all pages listed with correct URLs and lastModified dates. Visit /robots.txt and verify correct rules and sitemap reference.
--[ ] NAME:Test caching and revalidation DESCRIPTION:Make a content change in Sanity. Verify change appears after revalidation period or webhook trigger. Test that draft content doesn't appear in production.
--[ ] NAME:Test preview mode (if implemented) DESCRIPTION:Enable preview mode via API route. Verify draft content shows. Exit preview and verify published content returns.
--[ ] NAME:Test image loading DESCRIPTION:Verify all images from Sanity load correctly through Next Image component with proper optimization. Check different screen sizes and formats.
--[ ] NAME:Test error handling DESCRIPTION:Test 404 pages for non-existent slugs. Test error pages for runtime errors. Verify fallback content works when Sanity fields are missing.
--[ ] NAME:Test forms DESCRIPTION:Verify contact forms and other forms still work with labels/placeholders from Sanity. Test validation and submission.
--[ ] NAME:Verify zero hardcoded content DESCRIPTION:Search codebase for hardcoded English/Arabic strings. Verify all user-facing content comes from Sanity. Document any intentional exceptions (system messages).
--[ ] NAME:Editorial workflow testing DESCRIPTION:Have a non-technical user test Sanity Studio. Verify they can find and edit content easily. Check validation prevents broken pages. Document any UX improvements needed.
--[ ] NAME:Create editor documentation DESCRIPTION:Create /docs/sanity-editor-guide.md with instructions for editors: how to edit pages, add blog posts, update navigation, manage SEO, upload images, etc.
--[ ] NAME:Performance testing DESCRIPTION:Run Lighthouse audits on key pages. Verify performance scores remain high with Sanity integration. Check bundle size hasn't increased significantly.